import PlaceholderPage from "../components/PlaceholderPage";

export const metadata = { title: "Lease | South Property" };

export default function LeasePage() {
  return <PlaceholderPage title="Lease" subtitle="Discover premium rental properties in Australia's most sought-after locations." />;
}
