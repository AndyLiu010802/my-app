import PlaceholderPage from "../components/PlaceholderPage";

export const metadata = { title: "Sold | South Property" };

export default function SoldPage() {
  return <PlaceholderPage title="Sold" subtitle="A record of our successfully completed transactions and sales." />;
}
